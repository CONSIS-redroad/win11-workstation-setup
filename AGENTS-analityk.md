# Agent: patrz (skrot)

Pelna zasada: [AGENTS.md](AGENTS.md) — **prosta idea** + sekcja „Rozszerzenia”.

**Ten watek:** `diff`, `compare`, `compare-fleet`, `machines`, `list` — tylko JSON na stdout.

**Deliverable:** np. wyjasnienie `raport.dependencyGaps` albo `missingInProfile`.

**Nie:** `fill`, `add-*`, `pip-sync -Apply`, `upgrade`, `start.bat`.
